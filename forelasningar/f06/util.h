/** ask_question_string
 *
 *  Prompt the user to answer a question with a string
 *
 *  @arg q The question to be asked
 *
 *  @returns The entered string, allocated on the heap
 */
char *ask_question_string(char *q);
